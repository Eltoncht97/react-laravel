import React from "react";
import ReactDOM from "react-dom";
import { NarutoApp } from "./NarutoApp";

if (document.getElementById("root")) {
    ReactDOM.render(<NarutoApp />, document.getElementById("root"));
}
